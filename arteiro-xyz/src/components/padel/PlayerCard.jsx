import React from 'react'
import useProfile from './useProfile'

export default function PlayerCard(phoneNumber) {

    //const Profile = useProfile(phoneNumber)

  return (
    <>
        <div>PlayerCard</div>
        <> {phoneNumber} </>
    </>
  )
}
